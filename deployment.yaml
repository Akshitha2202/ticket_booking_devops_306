apiVersion: apps/v1
kind: Deployment
metadata:
  name: movie-ticket-booking-deployment
  labels:
    app: movie-ticket-booking
spec:
  replicas: 2
  selector:
    matchLabels:
      app: movie-ticket-booking
  template:
    metadata:
      labels:
        app: movie-ticket-booking
    spec:
      containers:
        - name: movie-ticket-booking
          image: YOUR_DOCKERHUB_USERNAME/movie-ticket-booking:latest
          ports:
            - containerPort: 80
